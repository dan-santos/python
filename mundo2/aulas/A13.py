for c in range(6, 0, -1): #-1 faz o for percorrer do final para o começo
    # por padrão, o terceiro argumento do for é 1, mas podemos substituir por outro inteiro para ele iterar "pulando
    # de x em x"
    print(c)
print('FIM')